
// define the structure here 

typedef struct _point {
    float x;
    float y;
    // your code
} Point;

// state function headers here,这里定义了一个结构体Point，包含两个float类型的成员x和y，表示点的坐标。接下来在.c文件中实现计算两点间距离的函数。

float distance( Point p, Point q );

